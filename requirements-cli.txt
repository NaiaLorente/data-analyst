# Dependencies for `python -m agent.cli` only (used by the GitHub Action) —
# same as requirements.txt minus streamlit, which the CLI never imports and
# which otherwise dominates install time in CI.
openai>=1.30.0
anthropic>=0.40.0
google-generativeai>=0.8.0
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.11.0
matplotlib>=3.8.0
seaborn>=0.13.0
openpyxl>=3.1.0
pyarrow>=14.0.0
